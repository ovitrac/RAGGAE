# Labels seed

Put small CSV/JSON label seeds here, e.g.:

- `tender_topics.csv` with columns: `doc_id, label` (DevOps, MLOps, Security…)
- `requirements.json` a curated list of requirement templates to reuse

This folder is intentionally empty in the scaffold.
